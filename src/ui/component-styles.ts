import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    fixedHeader: {
        position: "absolute",
        top: 0,
        left: 0,
        right: 0
    },

    parallaxHeader: {
        position: "absolute",
        top: 0,
        left: 0,
        right: 0,
        width: "100%",
        zIndex: 0
    }
});
